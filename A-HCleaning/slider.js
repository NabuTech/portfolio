$(document).ready(function() {
    $('#carouselTestimonials').carousel({
      interval: 3000, // Set the delay for each slide in milliseconds
      pause: 'hover', // Pause the carousel on mouse hover
      wrap: true, // Enable the carousel to wrap around
      keyboard: false, // Disable keyboard controls for the carousel
      touch: false // Disable touch controls for the carousel
    });
  });
  